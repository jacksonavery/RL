#pragma once
#include <string>
namespace globals {
	const int width = 1280, height = 720, tileSize = 40;
	const int tWidth = width / tileSize, tHeight = height / tileSize;
	const std::string font = "fonts/SourceHanSerif-VF.ttf";
}